server:
  applicationConnectors:
    - type: http
      port: 8080
  adminConnectors:
    - type: http
      port: 8081
logging:
  level: INFO
  loggers:
    "org.col": DEBUG
  appenders:
    - type: console
db:
  host: "${db.host}"
  port: 5432
  database: ${db.database}
  user: ${db.user}
  password: ${db.password}
auth:
  type: map
  users:
    - username: user
      password: 1234
      role:
    - username: editor
      password: 123456
      role: EDITOR
    - username: admin
      password: 12345678
      role: ADMIN
jwtKey: 3254e6rftgbzg6tz7
img:
  repo: ${img.repo}
