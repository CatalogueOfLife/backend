<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="life.catalogue.db.mapper.ArchivedNameUsageMatchMapper">

  <!-- NameMatch is collapsed to a bare nidx + matched flag; no IndexName association anymore -->
  <resultMap id="nameMatchResultMap" type="NameMatch" autoMapping="true"/>


  <select id="get" resultMap="nameMatchResultMap">
    SELECT nm.index_id AS nidx, nm.index_id IS NOT NULL AS matched
    FROM name_usage_archive_match nm
    WHERE nm.dataset_key = #{key.datasetKey} AND nm.usage_id = #{key.id}
  </select>

  <select id="processIndexIds" resultType="Integer" resultOrdered="true" fetchSize="10000" resultSetType="FORWARD_ONLY">
    SELECT DISTINCT index_id
    FROM name_usage_archive_match
    WHERE dataset_key = #{datasetKey}
  </select>

  <delete id="deleteOrphans" parameterType="map">
    DELETE FROM name_usage_archive_match nm
    WHERE dataset_key=#{datasetKey} AND NOT EXISTS (
      SELECT NULL FROM name_usage_archive n
      WHERE n.dataset_key=#{datasetKey} AND n.id = nm.usage_id
    )
  </delete>

  <delete id="deleteByDataset" parameterType="map">
    DELETE FROM name_usage_archive_match
    WHERE dataset_key=#{datasetKey}
  </delete>

  <insert id="create" parameterType="map" useGeneratedKeys="false">
    INSERT INTO name_usage_archive_match (dataset_key,index_id,usage_id)
    VALUES (#{key.datasetKey}, #{nidx}, #{key.id})
  </insert>

  <insert id="upsert" parameterType="map" useGeneratedKeys="false">
    INSERT INTO name_usage_archive_match (dataset_key,index_id,usage_id)
    VALUES (#{key.datasetKey}, #{nidx}, #{key.id})
    ON CONFLICT (dataset_key, usage_id) DO UPDATE SET index_id = EXCLUDED.index_id
  </insert>

  <update id="update" parameterType="map">
    UPDATE name_usage_archive_match SET index_id = #{nidx}
    WHERE dataset_key=#{key.datasetKey} AND usage_id = #{key.id}
  </update>

  <delete id="delete" parameterType="map">
    DELETE FROM name_usage_archive_match
    WHERE dataset_key=#{key.datasetKey} AND usage_id = #{key.id}
  </delete>

  <select id="countNidx" resultType="integer">
    SELECT count(*)
    FROM name_usage_archive_match nm
    WHERE nm.index_id=#{nidx}
    <if test="datasetKey != null">
      AND nm.dataset_key = #{datasetKey}
    </if>
  </select>

  <update id="truncate">
    TRUNCATE name_usage_archive_match
  </update>

  <!-- a supplying release may rewrite the match of any record it does not hold the version of (blocked by a higher
       ranked release); a non supplying release (ignored, private, or a lower ranked duplicate generation) only ever
       inserted the record in the first place, so it may only touch the match of the record it just inserted, never
       one whose version another release already supplied. -->
  <insert id="copyReleaseMatches" parameterType="map">
    INSERT INTO name_usage_archive_match (dataset_key, index_id, usage_id)
    SELECT #{projectKey}, nm.index_id, u.id
    FROM name_usage u
      JOIN name_match nm ON nm.dataset_key=u.dataset_key AND nm.name_id=u.name_id
      JOIN name_usage_archive a ON a.dataset_key=#{projectKey} AND a.id=u.id
    WHERE u.dataset_key=${releaseKey}
      AND <choose>
        <when test="insertedOnly">cardinality(a.release_keys) = 0</when>
        <otherwise>NOT (a.release_keys &amp;&amp; <include refid="life.catalogue.db.mapper.ArchivedNameUsageMapper.BLOCKING"/>)</otherwise>
      </choose>
    ON CONFLICT (dataset_key, usage_id) DO UPDATE SET index_id = EXCLUDED.index_id
      WHERE name_usage_archive_match.index_id IS DISTINCT FROM EXCLUDED.index_id
  </insert>

  <delete id="deleteUnmatchedReleaseMatches" parameterType="map">
    DELETE FROM name_usage_archive_match am
    USING name_usage u, name_usage_archive a
    WHERE am.dataset_key=#{projectKey}
      AND u.dataset_key=${releaseKey} AND u.id=am.usage_id
      AND a.dataset_key=#{projectKey} AND a.id=am.usage_id
      AND <choose>
        <when test="insertedOnly">cardinality(a.release_keys) = 0</when>
        <otherwise>NOT (a.release_keys &amp;&amp; <include refid="life.catalogue.db.mapper.ArchivedNameUsageMapper.BLOCKING"/>)</otherwise>
      </choose>
      AND NOT EXISTS (SELECT true FROM name_match nm WHERE nm.dataset_key=${releaseKey} AND nm.name_id=u.name_id)
  </delete>

</mapper>
