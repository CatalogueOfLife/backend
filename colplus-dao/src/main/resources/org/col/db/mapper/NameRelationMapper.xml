<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="org.col.db.mapper.NameRelationMapper">

  <sql id="SELECT">
    nr.key,
    nr.verbatim_key,
    nr.dataset_key,
    nr.type,
    nr.name_id,
    nr.related_name_id,
    nr.published_in_id,
    nr.note,
    nr.created,
    nr.created_by,
    nr.modified,
    nr.modified_by
  </sql>

  <sql id="COLS">
    verbatim_key,
    dataset_key,
    type,
    name_id,
    related_name_id,
    published_in_id,
    note,
    created_by,
    modified_by
  </sql>

  <sql id="PROPS">
    #{verbatimKey},
    #{datasetKey},
    #{type},
    #{nameId},
    #{relatedNameId},
    #{publishedInId},
    #{note},
    #{createdBy},
    #{modifiedBy}
  </sql>

  <sql id="FROM">
    name_rel nr
  </sql>

  <resultMap id="nameRelResultMap" type="NameRelation" autoMapping="true">
    <id property="key" column="key" />
  </resultMap>

  <select id="list" resultMap="nameRelResultMap">
    SELECT <include refid="SELECT" />
    FROM name_rel nr
    WHERE nr.dataset_key = #{datasetKey}
      AND (nr.name_id = #{nameId} OR nr.related_name_id = #{nameId})
    ORDER BY nr.key
  </select>

  <select id="listByType" resultMap="nameRelResultMap">
    SELECT <include refid="SELECT" />
    FROM name_rel nr
    WHERE nr.dataset_key = #{datasetKey}
      AND nr.type=#{type}
      AND nr.name_id = #{nameId}
    ORDER BY nr.key
  </select>

  <insert id="create" parameterType="NameRelation" useGeneratedKeys="true" keyProperty="key">
    INSERT INTO name_rel_${datasetKey} ( <include refid="COLS" />, created, modified )
    VALUES ( <include refid="PROPS" />, now(), now() )
  </insert>

</mapper>
