<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="org.col.db.mapper.SectorMapper">

  <sql id="SelectVar">
    ${alias}key AS ${prefix}key,
    ${alias}dataset_key AS ${prefix}dataset_key,
    ${alias}subject_id AS ${prefix}subject_id,
    ${alias}subject_name AS ${prefix}subject_name,
    ${alias}subject_authorship AS ${prefix}subject_authorship,
    ${alias}subject_rank AS ${prefix}subject_rank,
    ${alias}target_id AS ${prefix}target_id,
    ${alias}target_name AS ${prefix}target_name,
    ${alias}target_authorship AS ${prefix}target_authorship,
    ${alias}target_rank AS ${prefix}target_rank,
    ${alias}mode AS ${prefix}mode,
    ${alias}note AS ${prefix}note,
    ${alias}modified AS ${prefix}modified,
    ${alias}modified_by AS ${prefix}modified_by,
    ${alias}created AS ${prefix}created,
    ${alias}created_by AS ${prefix}created_by
  </sql>

  <sql id="SELECT">
    <include refid="org.col.db.mapper.SectorMapper.SelectVar">
      <property name="alias" value="s."/>
      <property name="prefix" value=""/>
    </include>
  </sql>

  <sql id="COLS">
    dataset_key,
    subject_id,
    subject_name,
    subject_authorship,
    subject_rank,
    target_id,
    target_name,
    target_authorship,
    target_rank,
    mode,
    note,
    modified,
    modified_by
  </sql>

  <sql id="PROPS">
    #{datasetKey},
    #{subject.id},
    #{subject.name},
    #{subject.authorship},
    #{subject.rank}::rank,
    #{target.id},
    #{target.name},
    #{target.authorship},
    #{target.rank}::rank,
    #{mode},
    #{note},
    now(),
    #{modifiedBy}
  </sql>

  <sql id="FROM">
    sector s
  </sql>

  <sql id="WHERE">
    <where>
      <if test="idIsNull">
        t.id IS NULL
      </if>
      <if test="datasetKey != null">
        AND s.dataset_key = #{datasetKey}
      </if>
    </where>
  </sql>

  <!-- A mapping to Distribution, mostly auto mapped -->
  <resultMap id="sectorResultMap" type="Sector" autoMapping="true">
    <id property="key" column="key" />
    <association property="subject" javaType="SimpleName" resultMap="org.col.db.mapper.SectorMapper.refResultMap" columnPrefix="subject_" />
    <association property="target" javaType="SimpleName" resultMap="org.col.db.mapper.SectorMapper.refResultMap" columnPrefix="target_" />
  </resultMap>

  <resultMap id="refResultMap" type="SimpleName" autoMapping="true">
  </resultMap>


  <select id="get" resultMap="sectorResultMap">
    SELECT <include refid="SELECT" />
    FROM <include refid="FROM" />
    WHERE key = #{key}
  </select>

  <select id="getBySubject" resultMap="sectorResultMap">
    SELECT <include refid="SELECT" />
    FROM <include refid="FROM" />
    WHERE dataset_key = #{datasetKey} AND subject_id = #{id}
  </select>

  <select id="listByTarget" resultMap="sectorResultMap">
    SELECT <include refid="SELECT" />
    FROM <include refid="FROM" />
    WHERE target_id = #{id}
  </select>

  <select id="processAll" parameterType="map" resultMap="sectorResultMap" resultOrdered="true" fetchSize="1000" resultSetType="FORWARD_ONLY">
    SELECT <include refid="SELECT" />
    FROM <include refid="FROM" />
    ORDER BY key
  </select>

  <insert id="create" parameterType="Sector" useGeneratedKeys="true" keyProperty="key">
    INSERT INTO sector ( <include refid="COLS" />, created_by )
    VALUES ( <include refid="PROPS" />, #{createdBy} )
  </insert>

  <update id="update" parameterType="Sector">
    UPDATE sector
    SET (<include refid="COLS" />) = (<include refid="PROPS" />)
    WHERE key = #{key}
  </update>

  <delete id="delete" parameterType="map">
    DELETE FROM sector
    WHERE key = #{key}
  </delete>

  <select id="list" resultMap="sectorResultMap">
    SELECT <include refid="SELECT" />
    FROM <include refid="FROM" />
    ORDER BY target_rank, key
  </select>

  <select id="listByDataset" resultMap="sectorResultMap">
    SELECT <include refid="SELECT" />
    FROM <include refid="FROM" />
    <bind name="idIsNull" value="false" />
    <include refid="WHERE" />
    ORDER BY target_rank, key
  </select>

  <select id="listChildSectors" resultMap="sectorResultMap">
    SELECT <include refid="SELECT" />
    FROM <include refid="FROM" />
      JOIN name_usage_3 t ON t.id=s.target_id
    WHERE t.sector_key=#{key}
    ORDER BY key
  </select>

  <select id="subjectBroken" resultMap="sectorResultMap">
    SELECT <include refid="SELECT" />
    FROM <include refid="FROM" />
      LEFT JOIN name_usage t ON t.id=s.subject_id AND t.dataset_key = #{datasetKey}
    <bind name="idIsNull" value="true" />
    <include refid="WHERE" />
    ORDER BY key
  </select>

  <select id="targetBroken" resultMap="sectorResultMap">
    SELECT <include refid="SELECT" />
    FROM <include refid="FROM" />
      LEFT JOIN name_usage_3 t ON t.id=s.target_id
    <bind name="idIsNull" value="true" />
    <include refid="WHERE" />
    ORDER BY key
  </select>

  <select id="processSectors" resultMap="sectorResultMap" parameterType="map" resultOrdered="true" fetchSize="10000" resultSetType="FORWARD_ONLY">
    SELECT <include refid="SELECT" />
    FROM <include refid="FROM" />
    ORDER BY key
  </select>

</mapper>
