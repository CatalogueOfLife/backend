<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="org.col.db.mapper.DescriptionMapper">

  <sql id="SELECT">
    d.key,
    d.taxon_id,
    d.verbatim_key,
    d.category,
    d.description,
    d.language,
    d.reference_id,
    d.created,
    d.created_by,
    d.modified,
    d.modified_by
  </sql>

  <sql id="COLS">
    dataset_key,
    taxon_id,
    verbatim_key,
    category,
    description,
    language,
    reference_id,
    created_by,
    modified_by
  </sql>

  <sql id="PROPS">
    #{datasetKey},
    #{taxonId},
    #{obj.verbatimKey},
    #{obj.category},
    #{obj.description},
    #{obj.language},
    #{obj.referenceId},
    #{obj.createdBy},
    #{obj.modifiedBy}
  </sql>

  <sql id="FROM">
    description_${datasetKey} d
  </sql>

  <resultMap id="descriptionResultMap" type="Description" autoMapping="true">
    <id property="key" column="key" />
  </resultMap>


  <select id="listByTaxon" resultMap="descriptionResultMap">
    SELECT <include refid="SELECT" />
    FROM <include refid="FROM" />
    WHERE d.taxon_id = #{taxonId}
    ORDER BY d.key
  </select>

  <select id="get" resultMap="descriptionResultMap">
    SELECT <include refid="SELECT" />
    FROM <include refid="FROM" />
    WHERE d.key = #{key}
  </select>

  <insert id="create" parameterType="Distribution" useGeneratedKeys="true" keyProperty="obj.key">
    INSERT INTO description_${datasetKey} ( <include refid="COLS" />, created, modified )
    VALUES ( <include refid="PROPS" />, now(), now() )
  </insert>

</mapper>
