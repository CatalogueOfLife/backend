<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="org.col.db.mapper.DistributionMapper">

  <sql id="SELECT">
    d.key,
    d.taxon_key,
    d.verbatim_key,
    d.area,
    d.gazetteer,
    d.status,
    dr.reference_key
  </sql>

  <sql id="COLS">
    dataset_key,
    taxon_key,
    verbatim_key,
    area,
    gazetteer,
    status
  </sql>

  <sql id="PROPS">
    #{datasetKey},
    #{taxonKey},
    #{d.verbatimKey},
    #{d.area},
    #{d.gazetteer},
    #{d.status}
  </sql>

  <sql id="FROM">
    distribution d LEFT JOIN distribution_reference dr ON d.key=dr.distribution_key
  </sql>

  <!-- A mapping to Distribution, mostly auto mapped -->
  <resultMap id="distributionResultMap" type="Distribution" autoMapping="true">
    <id property="key" column="key" />
    <collection property="referenceKeys" ofType="Integer">
        <result column="reference_key"/>
    </collection>
  </resultMap>


  <select id="listByTaxon" resultMap="distributionResultMap">
    SELECT <include refid="SELECT" />
    FROM <include refid="FROM" />
    WHERE d.taxon_key = #{taxonKey}
    ORDER BY d.key
  </select>

  <select id="get" resultMap="distributionResultMap">
    SELECT <include refid="SELECT" />
    FROM <include refid="FROM" />
    WHERE d.key = #{key}
  </select>

  <insert id="create" parameterType="Distribution" useGeneratedKeys="true" keyProperty="distribution.key">
    INSERT INTO distribution ( <include refid="COLS" /> )
    VALUES ( <include refid="PROPS" /> )
  </insert>

</mapper>
