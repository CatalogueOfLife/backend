<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="org.col.db.mapper.NameActMapper">

  <sql id="SELECT">
    na.key,
    na.dataset_key,
    na.type,
    na.status,
    na.name_key,
    na.related_name_key,
    na.description,
    na.reference_key,
    na.reference_page
  </sql>

  <sql id="COLS">
    dataset_key,
    type,
    status,
    name_key,
    related_name_key,
    description,
    reference_key,
    reference_page
  </sql>

  <sql id="PROPS">
    #{datasetKey},
    #{type},
    #{status},
    #{name.key},
    #{relatedName.key},
    #{description},
    #{reference.key},
    #{referencePage}
  </sql>

  <sql id="FROM">
    name_act na
  </sql>

  <resultMap id="nameActResultMap" type="NameAct" autoMapping="true">
    <id property="key" column="key" />
    <association property="name" javaType="Name">
      <id property="key" column="name_key" />
    </association>
    <association property="relatedName" javaType="Name">
      <id property="key" column="related_name_key" />
    </association>
    <association property="reference" javaType="Reference">
      <id property="key" column="reference_key" />
    </association>
  </resultMap>

  <select id="listByName" parameterType="map" resultMap="nameActResultMap">
    SELECT
    <include refid="SELECT" />
    FROM name_act na
    LEFT JOIN name n ON (na.name_key = n.key)
    WHERE na.dataset_key = #{datasetKey}
    AND n.id = #{nameId}
    ORDER BY na.key
  </select>

  <select id="listByHomotypicGroup" parameterType="map" resultMap="nameActResultMap">
    SELECT
    <include refid="SELECT" />
    FROM name_act as na
    LEFT JOIN name as name ON (na.name_key = name.key OR na.name_key = name.basionym_key)
    LEFT JOIN name as sibling ON (name.basionym_key = sibling.basionym_key AND name.key != sibling.key)
    WHERE na.dataset_key = #{datasetKey}
    AND (name.id = #{nameId} OR sibling.id = #{nameId})
    ORDER BY na.key
  </select>

  <select id="getByKey" resultMap="nameActResultMap">
    SELECT
    <include refid="SELECT" />
    FROM
    <include refid="FROM" />
    WHERE na.key = #{key}
  </select>

  <insert id="create" parameterType="NameAct" useGeneratedKeys="true" keyProperty="key">
    INSERT INTO name_act (
    <include refid="COLS" />
    )
    VALUES (
    <include refid="PROPS" />
    )
  </insert>

</mapper>
