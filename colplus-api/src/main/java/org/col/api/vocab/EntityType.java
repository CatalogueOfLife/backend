package org.col.api.vocab;

/**
 * Enumeration of major entities in the system.
 */
public enum EntityType {
  NAME,
  NAME_RELATION,
  TAXON,
  SYNONYM,
  DESCRIPTION,
  DISTRIBUTION,
  MEDIA,
  VERNACULAR,
  REFERENCE
}