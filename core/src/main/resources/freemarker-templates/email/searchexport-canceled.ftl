<#include "header.ftl">

Your ChecklistBank search download ${job.key} from dataset "${job.dataset.title}<#if job.dataset.version??>, version ${job.dataset.version}</#if>" was canceled.

<#include "footer.ftl">
