A TextTree where every node carries an explicit `ID` info item, so the usage ids are stable by themselves.
The *name* ids are still derived from the line number though. Paired with `9b`, the same tree with a family
inserted at the top. See https://github.com/CatalogueOfLife/backend/issues/1189
