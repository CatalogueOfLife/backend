A TextTree without any explicit `ID` info items, so every name and usage id is derived from the line number.
Paired with `8b`, the same tree with a family inserted at the top, which shifts every line below it and used
to give every record a new identifier. See https://github.com/CatalogueOfLife/backend/issues/1189
